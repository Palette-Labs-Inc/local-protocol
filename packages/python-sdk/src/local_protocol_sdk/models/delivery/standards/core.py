# generated by datamodel-codegen:
#   filename:  delivery/standards/core.json

from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field
from typing import Literal


class Event(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  description: str
  """
    Human-readable description of the event.
    """


class Pending(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  description: Literal["Job accepted, work not started"] = (
    "Job accepted, work not started"
  )


class Active(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  description: Literal["Work in progress"] = "Work in progress"


class Completed(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  description: Literal["Successfully finished"] = "Successfully finished"


class Failed(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  description: Literal["Unsuccessfully finished"] = "Unsuccessfully finished"


class Events1(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  pending: Pending | None = None
  active: Active | None = None
  completed: Completed | None = None
  failed: Failed | None = None


class Instance(BaseModel):
  """An instance of the core standard with event definitions."""

  model_config = ConfigDict(
    extra="allow",
  )
  name: Literal["xyz.localprotocol.delivery.core"] = (
    "xyz.localprotocol.delivery.core"
  )
  version: Literal["1.0.0"] = "1.0.0"
  events: Events1 | None = None


class Events(BaseModel):
  model_config = ConfigDict(
    extra="allow",
  )
  pending: Event
  active: Event
  completed: Event
  failed: Event


class DeliveryCoreStandard(BaseModel):
  """Core standard defining minimal universal events for delivery tracking. Optional baseline."""

  model_config = ConfigDict(
    extra="allow",
  )
  name: Literal["xyz.localprotocol.delivery.core"]
  version: str = Field(..., pattern="^\\d+\\.\\d+\\.\\d+$")
  """
    Semantic version (e.g., 1.0.0).
    """
  events: Events
