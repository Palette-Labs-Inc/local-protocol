# generated by datamodel-codegen:
#   filename:  payment/types/evm_token.json

from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field


class EvmToken(BaseModel):
  """EVM token identifier used for auth/capture settlement."""

  model_config = ConfigDict(
    extra="allow",
  )
  address: str | None = Field(None, pattern="^0x[a-fA-F0-9]{40}$")
  """
    ERC-20 contract address. Omit for native gas tokens (e.g., ETH, MATIC).
    """
  symbol: str
  """
    Token symbol (e.g., USDC).
    """
  decimals: int = Field(..., ge=0, le=255)
  """
    Token decimals.
    """
