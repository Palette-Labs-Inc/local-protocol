# generated by datamodel-codegen:
#   filename:  payment/evm_auth_capture_escrow_config.json

from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field
from .types import evm_token


class EvmAuthCaptureEscrowConfig(BaseModel):
  """Handler configuration for auth/capture escrow on EVM chains."""

  model_config = ConfigDict(
    extra="allow",
  )
  chain_id: int = Field(..., ge=1)
  """
    EVM chain id for the escrow contract.
    """
  contract: str = Field(..., pattern="^0x[a-fA-F0-9]{40}$")
  """
    Escrow contract address on the target chain.
    """
  operator: str = Field(..., pattern="^0x[a-fA-F0-9]{40}$")
  """
    Operator address authorized to drive state transitions.
    """
  receiver: str = Field(..., pattern="^0x[a-fA-F0-9]{40}$")
  """
    Default receiver address for captures.
    """
  accepted_tokens: list[evm_token.EvmToken] = Field(..., min_length=1)
  """
    Tokens accepted on the escrow contract chain.
    """
