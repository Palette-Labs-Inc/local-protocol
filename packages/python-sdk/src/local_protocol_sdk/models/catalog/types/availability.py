# generated by datamodel-codegen:
#   filename:  catalog/types/availability.json

from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field
from . import interval


class Availability(BaseModel):
  """Availability schedule for a catalog, category, or item."""

  model_config = ConfigDict(
    extra="allow",
  )
  timezone: str | None = None
  """
    IANA timezone for the intervals. Defaults to the merchant timezone when omitted.
    """
  intervals: list[interval.Interval] = Field(..., min_length=1)
  """
    Availability intervals (weekly or date-specific).
    """
