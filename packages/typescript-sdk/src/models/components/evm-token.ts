/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * EVM token identifier used for auth/capture settlement.
 */
export type EvmToken = {
  /**
   * ERC-20 contract address. Omit for native gas tokens.
   */
  address?: string | undefined;
  /**
   * Token symbol (e.g., USDC).
   */
  symbol: string;
  /**
   * Token decimals.
   */
  decimals: number;
};

/** @internal */
export const EvmToken$inboundSchema: z.ZodMiniType<EvmToken, unknown> = z
  .object({
    address: types.optional(types.string()),
    symbol: types.string(),
    decimals: types.number(),
  });
/** @internal */
export type EvmToken$Outbound = {
  address?: string | undefined;
  symbol: string;
  decimals: number;
};

/** @internal */
export const EvmToken$outboundSchema: z.ZodMiniType<
  EvmToken$Outbound,
  EvmToken
> = z.object({
  address: z.optional(z.string()),
  symbol: z.string(),
  decimals: z.int(),
});

export function evmTokenToJSON(evmToken: EvmToken): string {
  return JSON.stringify(EvmToken$outboundSchema.parse(evmToken));
}
export function evmTokenFromJSON(
  jsonString: string,
): SafeParseResult<EvmToken, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvmToken$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvmToken' from JSON`,
  );
}
