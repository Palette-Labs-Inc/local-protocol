/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { Availability, Availability$inboundSchema } from "./availability.js";
import { CatalogItem, CatalogItem$inboundSchema } from "./catalog-item.js";

/**
 * A category grouping items in a catalog.
 */
export type CatalogCategory = {
  /**
   * Category identifier.
   */
  id: string;
  /**
   * Category display name.
   */
  name: string;
  /**
   * Optional category description.
   */
  description?: string | undefined;
  /**
   * Ordered child categories for nested category trees.
   */
  categories?: Array<CatalogCategory> | undefined;
  /**
   * Ordered items in this category.
   */
  items: Array<CatalogItem>;
  /**
   * Availability schedule for a catalog, category, or item.
   */
  availability?: Availability | undefined;
  /**
   * Business-defined custom data.
   */
  metadata?: { [k: string]: any } | undefined;
};

/** @internal */
export const CatalogCategory$inboundSchema: z.ZodMiniType<
  CatalogCategory,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
  description: types.optional(types.string()),
  categories: types.optional(
    z.array(z.lazy(() => CatalogCategory$inboundSchema)),
  ),
  items: z.array(CatalogItem$inboundSchema),
  availability: types.optional(Availability$inboundSchema),
  metadata: types.optional(z.record(z.string(), z.any())),
});

export function catalogCategoryFromJSON(
  jsonString: string,
): SafeParseResult<CatalogCategory, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CatalogCategory$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CatalogCategory' from JSON`,
  );
}
