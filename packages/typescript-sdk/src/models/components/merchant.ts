/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { Catalog, Catalog$inboundSchema } from "./catalog.js";

/**
 * Merchant catalog payload containing denormalized catalogs.
 */
export type Merchant = {
  /**
   * Merchant identifier.
   */
  id: string;
  /**
   * Merchant name.
   */
  name: string;
  /**
   * IANA timezone for availability schedules.
   */
  timezone: string;
  /**
   * RFC 3339 timestamp of the latest catalog update.
   */
  lastUpdated?: Date | undefined;
  /**
   * Catalogs available for the merchant.
   */
  catalogs: Array<Catalog>;
  /**
   * Business-defined custom data.
   */
  metadata?: { [k: string]: any } | undefined;
};

/** @internal */
export const Merchant$inboundSchema: z.ZodMiniType<Merchant, unknown> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    timezone: types.string(),
    last_updated: types.optional(types.date()),
    catalogs: z.array(Catalog$inboundSchema),
    metadata: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "last_updated": "lastUpdated",
    });
  }),
);

export function merchantFromJSON(
  jsonString: string,
): SafeParseResult<Merchant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Merchant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Merchant' from JSON`,
  );
}
