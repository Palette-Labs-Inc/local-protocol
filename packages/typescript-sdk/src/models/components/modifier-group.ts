/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  ModifierOption,
  ModifierOption$inboundSchema,
} from "./modifier-option.js";

/**
 * Group of modifier options with selection constraints.
 */
export type ModifierGroup = {
  /**
   * Modifier group identifier.
   */
  id: string;
  /**
   * Display name for the modifier group.
   */
  name: string;
  /**
   * Optional modifier group description.
   */
  description?: string | undefined;
  /**
   * Minimum selections required.
   */
  minimumSelections?: number | undefined;
  /**
   * Maximum selections allowed.
   */
  maximumSelections?: number | undefined;
  /**
   * Whether options can be selected with quantities > 1.
   */
  allowQuantities?: boolean | undefined;
  /**
   * Maximum quantity per modifier option.
   */
  maxPerModifier: number;
  /**
   * Ordered modifier options within this group.
   */
  modifierOptions: Array<ModifierOption>;
  /**
   * Modifier group type classification.
   */
  type?: string | undefined;
  /**
   * Business-defined custom data.
   */
  metadata?: { [k: string]: any } | undefined;
};

/** @internal */
export const ModifierGroup$inboundSchema: z.ZodMiniType<
  ModifierGroup,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    description: types.optional(types.string()),
    minimum_selections: types.optional(types.number()),
    maximum_selections: types.optional(types.number()),
    allow_quantities: types.optional(types.boolean()),
    max_per_modifier: z._default(types.number(), 1),
    modifier_options: z.array(z.lazy(() => ModifierOption$inboundSchema)),
    type: types.optional(types.string()),
    metadata: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "minimum_selections": "minimumSelections",
      "maximum_selections": "maximumSelections",
      "allow_quantities": "allowQuantities",
      "max_per_modifier": "maxPerModifier",
      "modifier_options": "modifierOptions",
    });
  }),
);

export function modifierGroupFromJSON(
  jsonString: string,
): SafeParseResult<ModifierGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModifierGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModifierGroup' from JSON`,
  );
}
