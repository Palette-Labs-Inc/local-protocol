/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Service discovery metadata.
 */
export type DiscoveryResponse = {
  /**
   * Protocol version.
   */
  version: string;
  /**
   * Server name.
   */
  name: string;
  /**
   * Supported capabilities by domain.
   */
  capabilities: { [k: string]: any };
  /**
   * Endpoint path map.
   */
  endpoints: { [k: string]: string };
};

/** @internal */
export const DiscoveryResponse$inboundSchema: z.ZodMiniType<
  DiscoveryResponse,
  unknown
> = z.object({
  version: types.string(),
  name: types.string(),
  capabilities: z.record(z.string(), z.any()),
  endpoints: z.record(z.string(), types.string()),
});

export function discoveryResponseFromJSON(
  jsonString: string,
): SafeParseResult<DiscoveryResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DiscoveryResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DiscoveryResponse' from JSON`,
  );
}
