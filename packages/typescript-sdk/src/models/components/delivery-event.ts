/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * A single delivery event definition.
 */
export type DeliveryEvent = {
  /**
   * Human-readable description of the event.
   */
  description: string;
};

/** @internal */
export const DeliveryEvent$inboundSchema: z.ZodMiniType<
  DeliveryEvent,
  unknown
> = z.object({
  description: types.string(),
});

export function deliveryEventFromJSON(
  jsonString: string,
): SafeParseResult<DeliveryEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeliveryEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeliveryEvent' from JSON`,
  );
}
