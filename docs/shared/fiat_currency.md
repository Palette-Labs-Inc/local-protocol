# FiatCurrency

Fiat currency descriptor used in [Amount](amount.md). Schema: `schemas/shared/fiat_currency.json`.

## Fields

- `symbol` (string, required): ISO 4217 currency code (e.g., `USD`, `EUR`, `JPY`).

## Example

```json
{ "symbol": "USD" }
```
