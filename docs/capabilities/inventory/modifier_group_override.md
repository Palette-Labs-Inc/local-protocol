# Modifier Group Override (Legacy)

Modifier group overrides were part of the normalized catalog model. In the denormalized catalog, items embed complete modifier groups directly, so overrides are no longer required.

If you need per-item differences, copy the modifier group into the item and edit it directly.
