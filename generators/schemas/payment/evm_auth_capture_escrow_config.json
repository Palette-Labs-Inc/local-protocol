{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localprotocol.xyz/schemas/payment/evm_auth_capture_escrow_config.json",
  "title": "EVM Auth/Capture Escrow Config",
  "description": "Handler configuration for auth/capture escrow on EVM chains.",
  "type": "object",
  "required": [
    "chain_id",
    "contract",
    "operator",
    "receiver",
    "accepted_tokens"
  ],
  "properties": {
    "chain_id": {
      "type": "integer",
      "description": "EVM chain id for the escrow contract.",
      "minimum": 1
    },
    "contract": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{40}$",
      "description": "Escrow contract address on the target chain."
    },
    "operator": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{40}$",
      "description": "Operator address authorized to drive state transitions."
    },
    "receiver": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{40}$",
      "description": "Default receiver address for captures."
    },
    "accepted_tokens": {
      "type": "array",
      "description": "Tokens accepted on the escrow contract chain.",
      "items": {
        "$ref": "types/evm_token.json"
      },
      "minItems": 1
    }
  },
  "additionalProperties": true
}
