{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localprotocol.xyz/schemas/commerce/types/availability_interval.json",
  "title": "Availability Interval",
  "description": "A single availability interval for a day of the week.",
  "type": "object",
  "required": ["day", "from_hour", "from_minute", "to_hour", "to_minute"],
  "properties": {
    "day": {
      "type": "string",
      "description": "Day of week (e.g., Monday, Tuesday)."
    },
    "from_hour": {
      "type": "integer",
      "minimum": 0,
      "maximum": 23,
      "description": "Start hour (0-23)."
    },
    "from_minute": {
      "type": "integer",
      "minimum": 0,
      "maximum": 59,
      "description": "Start minute (0-59)."
    },
    "to_hour": {
      "type": "integer",
      "minimum": 0,
      "maximum": 23,
      "description": "End hour (0-23)."
    },
    "to_minute": {
      "type": "integer",
      "minimum": 0,
      "maximum": 59,
      "description": "End minute (0-59)."
    }
  },
  "additionalProperties": false
}
